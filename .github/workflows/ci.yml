name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  pull_request_target:
    types: [opened, reopened, edited, synchronize]

jobs:
  semantic-pr-title:
    name: PR title
    if: github.event_name == 'pull_request_target'
    runs-on: ubuntu-24.04
    permissions:
      pull-requests: read
    steps:
      - uses: amannn/action-semantic-pull-request@v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          headerPattern: '^(\w+)(?:\((#[1-9][0-9]*)\))?: (.+)$'
          headerPatternCorrespondence: type, scope, subject
          requireScope: true
          scopes: |
            #[1-9][0-9]*
          types: |
            feat
            fix
            docs
            chore
            refactor
            test
            build
            ci
            perf
            style
            revert
          subjectPattern: ^[A-Z].+$
          subjectPatternError: |
            PR title must be `type(#123): Description`, e.g.
            `ci(#15): Preserve CI test results`.

  build:
    name: Build
    if: github.event_name != 'pull_request_target'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Restore
        run: dotnet restore NodeRunner.slnx

      - name: Build
        run: dotnet build NodeRunner.slnx --configuration Release --no-restore

  test:
    name: Test & coverage
    if: github.event_name != 'pull_request_target'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Restore
        run: dotnet restore NodeRunner.slnx

      - name: Test with coverage
        run: >
          dotnet test NodeRunner.slnx
          --configuration Release
          --no-restore
          --logger "trx;LogFilePrefix=test-results"
          --collect:"XPlat Code Coverage"
          --results-directory ./TestResults

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results
          path: TestResults/**/*.trx

      - name: Upload coverage
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage
          path: TestResults/**/coverage.cobertura.xml

  format:
    name: Format check
    if: github.event_name != 'pull_request_target'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Restore
        run: dotnet restore NodeRunner.slnx

      - name: Verify formatting
        run: dotnet format NodeRunner.slnx --verify-no-changes --no-restore
