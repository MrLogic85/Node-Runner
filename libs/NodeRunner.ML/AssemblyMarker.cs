namespace NodeRunner.ML;

/// <summary>
/// Type used as a compile-time reference from test/arch code to load this assembly.
/// Keep this class empty; it exists only so <c>typeof(AssemblyMarker).Assembly</c> works.
/// </summary>
public sealed class AssemblyMarker
{
}
