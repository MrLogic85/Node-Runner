Three cards across; the list scrolls sideways. Each card is a live thumbnail, a name, one line of stats ("142 gens · 18.4 m") and, only when the creature has progress, a thin bar toward its next unlock. The last-opened creature has an `accent` border.

The whole card opens the creature and resumes training. Two 48px icons sit in the card's bottom corners: **Duplicate** (asks Copy brain or Start fresh) and **Delete** (asks, then offers a 10 s Undo). The example creature carries an "Example" tag and a lock instead of Delete. **Restore example** lives in the overflow menu, not on the screen.

There is no Save button; the header says "Saved".